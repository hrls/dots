### Flying windows, detached tabs

These files should be mapped as:
 - `{init,early-init}.el` -> `~/.emacs.d/`
 - `*-theme.el` -> `~/.emacs.d/` (see description of `custom-theme-directory` variable)
 - `*.el` not listed above -> `~/.emacs.d/elisp/`
