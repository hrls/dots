# vim: set ft=zsh:

alias d=docker
alias dm=docker-machine

denv() { eval $(docker-machine env $1) }

alias drun='docker run -it --rm'
alias contgc='docker container rm -f $(docker container ls -qa)'
